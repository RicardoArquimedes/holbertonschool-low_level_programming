#include "holberton.h"

/**
 *print_square - print square
 *Return: Always 0
 *@size: integer
*/

void print_square(int size)
{
	int i, j;

	for (i = 1; i <= size; i++)
	{
		for (j = 1; j <= size; j++)
		{
			if (i == 1 || i == size || j == 1 || j == size || i == j || size + 1)
			{
				_putchar('#');
				else
				{
					_putchar(' ');
				}
			}
		}
	}
	_putchar('\n');
}

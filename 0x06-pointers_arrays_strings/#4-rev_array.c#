#include "holberton.h"
/**
 *
 *
 */

void reverse_array(int *a, int n)

{
	int p, length;
	
	n = n -1
	for (p = 0; p < length; p++)
        {
                n = a[p];
                a[p] = a[length - 1];
                a[length - 1] = n;
                length--;
        }


}
	

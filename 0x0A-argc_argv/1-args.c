#include <stdlib.h>
#include <stdio.h>

/**
 * main - entry point number arguments
 * @argc: number arg
 * @argv: pointer
 * Return: 0 (Always Succces)
 */
int main(int argc, char *argv[]__attribute__((unused)))
{
	printf("%d\n", argc - 1);
	return (0);
}
